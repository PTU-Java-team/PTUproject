<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "ItemMapper">
    <insert id="insertItem" parameterType="com.PTUproj.dto.ItemDTO">
        insert into product_table (product_name, product_img1, product_img2, product_description, product_price, category_ID)
        values (#{productName}, #{productImg1}, #{productImg2}, #{productDescription}, #{productPrice}, #{categoryId})
    </insert>

    <select id="selectAllItems" resultType="com.PTUproj.dto.ItemDTO">
        select product_id, product_name, product_img1, product_img2, product_description,product_price, product_date, category_id from product_table;
    </select>

    <select id="getItemDetail" parameterType="int" resultType="com.PTUproj.dto.ItemDTO">
        select product_id, product_name, product_img1, product_img2, product_description,product_price, product_date, category_id from product_table where product_id = #{productId}
    </select>
</mapper>