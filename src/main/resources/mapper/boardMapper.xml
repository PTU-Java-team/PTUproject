<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">

    <insert id="save" parameterType="board">
        insert into board_table(board_Writer, board_Pass, board_Title, board_Content)
        values(#{boardWriter}, #{boardPass}, #{boardTitle}, #{boardContent})
    </insert>


    <select id="findAll" resultType="board">
        select * from board_table order by board_id desc
    </select>


    <select id="findById" parameterType="Long" resultType="board">
        select * from board_table where board_id=#{id}
    </select>


    <delete id="delete" parameterType="Long">
        delete from board_table where board_id=#{id}
    </delete>


    <update id="update" parameterType="board">
        update board_table set board_Title=#{boardTitle}, board_Content=#{boardContent}
        where board_id=#{id}
    </update>


    <select id="pagingList" parameterType="java.util.HashMap" resultType="board">
        SELECT *
        FROM BOARD_TABLE
        ORDER BY board_id DESC
        OFFSET 0 ROWS FETCH NEXT 10 ROWS ONLY
    </select>

</mapper>


